const LoadingSpinner = () => {
  return (
    <main className="flex items-center justify-center h-screen">
      <div className="animate-spin rounded-full size-16 border-t-2 border-b-2 border-blue-500"></div>
    </main>
  );
};

export default LoadingSpinner;
